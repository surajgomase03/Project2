apiVersion: v1
kind: Service
metadata:
  name: go_service
  namespace: default
spec:
  selector:
    app: goapp
  type: NodePort
  ports:
  - name: go_port
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30007